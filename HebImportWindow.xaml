<Window x:Class="PersonalInventory.HebImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import H-E-B Shopping List" Height="560" Width="900"
        WindowStartupLocation="CenterOwner" Background="#F4F6F9">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtListTitle" Grid.Row="0" FontSize="20" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBlock x:Name="TxtItemCount" Grid.Row="1" Margin="0,0,0,10" Foreground="#4B5563"/>

        <DataGrid x:Name="GridImport" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False"
                  HeadersVisibility="Column" Background="White" RowHeight="34">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Import" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="65"/>
                <DataGridTextColumn Header="H-E-B Item" Binding="{Binding Name}" IsReadOnly="True" Width="3*"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" IsReadOnly="True" Width="70"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" IsReadOnly="True" Width="1.5*"/>
                <DataGridComboBoxColumn Header="Storage" SelectedItemBinding="{Binding Location, UpdateSourceTrigger=PropertyChanged}" Width="110"
                                        ItemsSource="{Binding Source={x:Static Application.Current}}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="90" Height="34" Margin="0,0,8,0" Click="BtnCancel_Click"/>
            <Button Content="Import Selected" Width="130" Height="34" Background="#2563EB" Foreground="White" FontWeight="Bold" BorderThickness="0" Click="BtnImport_Click"/>
        </StackPanel>
    </Grid>
</Window>
