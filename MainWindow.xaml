<Window x:Class="PersonalInventory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Personal Inventory &amp; Pantry Manager" Height="700" Width="1050"
        WindowStartupLocation="CenterScreen" Background="#F4F6F9">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,15,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="LightGray" Opacity="0.4"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Add / Restock Item" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333333"/>
                <TextBlock Text="Item Name" FontWeight="SemiBold" Margin="0,0,0,3"/>
                <TextBox x:Name="TxtName" Height="30" Padding="5" Margin="0,0,0,12"/>
                <TextBlock Text="Storage Location" FontWeight="SemiBold" Margin="0,0,0,3"/>
                <ComboBox x:Name="CmbLocation" Height="30" Margin="0,0,0,12"/>
                <TextBlock Text="Quantity" FontWeight="SemiBold" Margin="0,0,0,3"/>
                <TextBox x:Name="TxtQuantity" Height="30" Padding="5" Margin="0,0,0,12" Text="1"/>
                <TextBlock Text="Unit (e.g., pcs, lbs, cartons)" FontWeight="SemiBold" Margin="0,0,0,3"/>
                <TextBox x:Name="TxtUnit" Height="30" Padding="5" Margin="0,0,0,12" Text="pcs"/>
                <TextBlock Text="Low-Stock Minimum Threshold" FontWeight="SemiBold" Margin="0,0,0,3"/>
                <TextBox x:Name="TxtThreshold" Height="30" Padding="5" Margin="0,0,0,15" Text="1"/>
                <Button Content="Save to Inventory" Height="35" Background="#2563EB" Foreground="White"
                        FontWeight="Bold" BorderThickness="0" Click="BtnSave_Click" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="All Items" Width="90" Height="32" Margin="0,0,8,0" Click="BtnFilterAll_Click" Background="#E2E8F0" BorderThickness="0"/>
                <Button Content="Pantry" Width="75" Height="32" Margin="0,0,8,0" Click="BtnFilterLocation_Click" Tag="Pantry" Background="#E2E8F0" BorderThickness="0"/>
                <Button Content="Fridge" Width="75" Height="32" Margin="0,0,8,0" Click="BtnFilterLocation_Click" Tag="Fridge" Background="#E2E8F0" BorderThickness="0"/>
                <Button Content="Freezer" Width="75" Height="32" Margin="0,0,8,0" Click="BtnFilterLocation_Click" Tag="Freezer" Background="#E2E8F0" BorderThickness="0"/>
                <Button Content="Shopping List" Width="110" Height="32" Margin="0,0,8,0" Click="BtnFilterShopping_Click" Background="#FEE2E2" Foreground="#991B1B" FontWeight="Bold" BorderThickness="0"/>
                <Button Content="Export CSV" Width="90" Height="32" Margin="8,0,0,0" Click="BtnExport_Click" Background="#059669" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
            </StackPanel>

            <Border Grid.Row="1" Background="White" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="130"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="H-E-B List:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1" x:Name="TxtHebUrl" Height="30" Padding="5"
                             Text="https://www.heb.com/shopping-list/shared/"/>
                    <Button Grid.Column="2" x:Name="BtnImportHeb" Content="Import H-E-B List" Height="30" Margin="10,0,0,0"
                            Background="#FF7A00" Foreground="White" FontWeight="Bold" BorderThickness="0" Click="BtnImportHeb_Click"/>
                </Grid>
            </Border>

            <DataGrid x:Name="GridInventory" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="White" RowHeight="32">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*"/>
                    <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="*"/>
                    <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="*"/>
                    <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="*"/>
                    <DataGridTextColumn Header="Min Threshold" Binding="{Binding MinimumThreshold}" Width="*"/>
                    <DataGridCheckBoxColumn Header="On Shopping List" Binding="{Binding IsMarkedForShopping, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                    <DataGridTemplateColumn Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Toggle List" Click="BtnToggleShopping_Click" Padding="4,2" Background="#EDF2F7" BorderThickness="1" BorderBrush="#CBD5E0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>
